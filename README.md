# Prisma-ORM


npm init -y (add package.json file)
npm install typescript (install typescript)
npx tsc --init (create a tsconfig file)
change rootDir to src 
change outDir to  dist 
create a src folder and add index.ts


  "scripts": {
    "dev": "tsc -b && node ./dist/index.js"
  },